<%= form_with(model: [test, question, choice], url: test_question_choices_path(test, question), method: :post) do |form| %>
  <div>
    <%= form.label :choice %>
    <%= form.text_field :choice %>
  </div>
  
  <%= form.submit %>
<% end %>